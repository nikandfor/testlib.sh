# testlib.sh

testlib.sh is a tiny library for running quick tests.

It makes test output to look like it was interactive session of run command, see output.
Commands are printed properly quoted, so if you want to rerun one command, you can simply copy it, paste, and run.
Plus it colorizes the output.

## Dependencies

* bash
* curl
* jq
